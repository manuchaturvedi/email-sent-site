# Environment variables for Render deployment
# Add these to your Render app configuration:

# Required for Flask production
FLASK_ENV=production

# Chrome configuration for headless deployment
CHROME_BIN=/usr/bin/google-chrome-stable
CHROMEDRIVER_PATH=/usr/bin/chromedriver

# Python path
PYTHONPATH=/opt/render/project/src

# Disable Chrome sandbox for container deployment
CHROME_NO_SANDBOX=true

# Firebase credentials (add your JSON as base64 encoded string)
# GOOGLE_APPLICATION_CREDENTIALS_JSON=<your-firebase-json-base64>